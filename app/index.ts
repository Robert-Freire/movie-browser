import 'angular';

import './app.module';

import 'bootstrap/dist/css/bootstrap.css';
import '../css/app.css';

/*angular.element(document).ready(() => {
	angular.bootstrap(document, ['app'], {
		strictDi: true
	});
});*/